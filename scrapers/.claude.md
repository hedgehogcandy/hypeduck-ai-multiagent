# 🕷️ 스크래퍼 개발 가이드

> 데이터 수집 스크립트 개발 규칙 및 아키텍처

---

## 🎯 핵심 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│  🔴 절대 규칙: Chrome MCP와 스크래퍼 역할 분리!                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Chrome MCP (계정 보호)          Backend Scraper (데이터 처리)   │
│  ─────────────────────          ─────────────────────────────   │
│  ✅ 로그인                       ✅ HTML 파싱                    │
│  ✅ 페이지 이동                  ✅ JSON 추출                    │
│  ✅ 스크롤                       ✅ 이미지 다운로드               │
│  ✅ 클릭                         ✅ API 호출                     │
│  ✅ 폼 입력                      ✅ 데이터 저장                   │
│                                                                  │
│  ❌ 데이터 파싱 금지!            ❌ 직접 브라우저 조작 금지!      │
│  ❌ API 호출 금지!               ❌ 로그인 처리 금지!             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 폴더 구조

```
scrapers/
├── python/
│   ├── instagram/
│   │   ├── profile_parser.py    # 프로필 파싱
│   │   ├── post_parser.py       # 포스트 파싱
│   │   ├── comment_parser.py    # 댓글 파싱
│   │   └── metrics_calc.py      # 지표 계산
│   ├── sources/
│   │   ├── taobao.py            # 타오바오
│   │   ├── 1688.py              # 1688
│   │   ├── xiaohongshu.py       # 샤오홍슈
│   │   └── google_lens.py       # 구글 렌즈
│   └── utils/
│       ├── rate_limiter.py      # 속도 제한
│       ├── proxy_manager.py     # 프록시 관리
│       └── data_cleaner.py      # 데이터 정제
└── typescript/
    ├── instagram/
    │   └── ...
    └── utils/
        └── ...
```

---

## 🔧 개발 규칙

### Rate Limiting 필수

```python
# ❌ 나쁜 예
for url in urls:
    data = fetch(url)  # 무한 루프

# ✅ 좋은 예
from utils.rate_limiter import RateLimiter

limiter = RateLimiter(
    requests_per_minute=10,
    min_delay=3,
    max_delay=8,
    randomize=True
)

for url in urls:
    limiter.wait()
    data = fetch(url)
```

### 에러 핸들링

```python
# 재시도 로직 필수
@retry(max_attempts=3, backoff=exponential)
def fetch_data(url):
    try:
        response = requests.get(url)
        response.raise_for_status()
        return response.json()
    except Exception as e:
        log_error(e)
        raise
```

### 데이터 저장

```python
# 항상 JSON으로 저장
def save_data(data, path):
    with open(path, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
    
    # Git 커밋 (선택)
    git_commit(path, f"Update {path}")
```

---

## 🌐 영상 소스 스크래퍼

### 지원 플랫폼

| 플랫폼 | 파일 | 기능 |
|--------|------|------|
| 타오바오 | taobao.py | 이미지 검색, 상품 정보 |
| 1688 | 1688.py | 도매 상품, 영상 소스 |
| 샤오홍슈 | xiaohongshu.py | 트렌드, 영상 소스 |
| 구글 렌즈 | google_lens.py | 이미지 역검색 |
| 칼로데이터 | calodata.py | 이커머스 데이터 |

### 키워드 번역 (샤오홍슈용)

```python
def translate_keyword(keyword, source='ko', target='zh'):
    # Google Translate API 또는 Papago
    return translated_keyword
```

---

## 📊 데이터 스키마

### 인스타 프로필
```json
{
  "username": "string",
  "follower_count": "number",
  "following_count": "number",
  "post_count": "number",
  "bio": "string",
  "profile_pic_url": "string",
  "is_verified": "boolean",
  "scraped_at": "datetime"
}
```

### 인스타 포스트
```json
{
  "post_id": "string",
  "username": "string",
  "type": "reel|post|story",
  "view_count": "number",
  "like_count": "number",
  "comment_count": "number",
  "engagement_ratio": "number",
  "caption": "string",
  "hashtags": ["string"],
  "scraped_at": "datetime"
}
```

---

## ⚠️ 주의사항

1. **비공식 API 사용 주의**
   - 계정 밴 위험
   - 법적 이슈 가능

2. **개인정보 수집 금지**
   - 공개 데이터만 수집
   - DM, 비공개 정보 금지

3. **저장 전 정제**
   - 민감 정보 제거
   - 불필요 데이터 삭제

---

*Last Updated: 2026-02-03*
